using System;

namespace PostgreSQL.CodeFirst.Example.Constants;

internal static class TableNames
{
    internal const string Products = nameof(Products);
    internal const string Categories = nameof(Categories);
}   